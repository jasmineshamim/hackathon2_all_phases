/** @type {import('next').NextConfig} */
const nextConfig = {
  // appDir is now enabled by default in Next.js 13.4+ when using the app directory
  // No need to explicitly enable it anymore
};

module.exports = nextConfig;